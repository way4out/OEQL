[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

# Attribution: 4 GOD & 4 huMan
#
# This project was NOT compiled or tested in the sandbox that wrote it
# (no network access to install forge/solc). Run `forge install
# OpenZeppelin/openzeppelin-contracts` then `forge build` and
# `forge test` on your own machine BEFORE trusting any of this code,
# and before any deployment. Per this project's own governance rules
# (governance/human-authorization-gate.md), nothing here is
# authorized for mainnet deployment until: (1) it compiles and passes
# its own test suite, (2) an external security audit is complete.
